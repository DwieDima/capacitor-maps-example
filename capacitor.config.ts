import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.codify.maptest2',
  appName: 'maps-example',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
